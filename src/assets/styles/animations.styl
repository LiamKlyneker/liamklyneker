@keyframes animateLogo
	0%
		opacity 1
	50%
		opacity 0
	100%
		opacity 1
@keyframes introHeader
	from
		opacity 0
		transform translateY(-10px)
	to
		opacity 1
		transform translateY(0)
@keyframes hideBack
	from
		opacity 1
		transform scale(1)
	to
		opacity 0
		transform scale(1.1)
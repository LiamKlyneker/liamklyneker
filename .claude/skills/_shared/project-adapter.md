# Project Adapter — liamklyneker (portfolio site)

Single home for every project-specific fact the skills need. Workflow skills (`work-on-prd`, `to-issues`, `next-prd-issue`, `work-on-issue`) and `grill-me` reference this file and never hardcode these values.

**Canonical source:** skill *logic* is canonical in `LiamKlyneker/skills` and reaches this repo by **copy, not symlink** — an improvement made here is drift until it's back-ported there. The project facts below live only in this file.

## Repo

- Issue tracker / PRs: `LiamKlyneker/liamklyneker` (GitHub, via `gh`)
- Default branch: `main` (PRs must target it — `Closes` keywords only fire against the default branch)
- Related repos (cross-repo issues, API contracts): **None.** `LiamKlyneker/skills` is a *content source* for the skills in `.claude/skills/`, not an API-contract boundary — nothing in this repo compiles against it.

## Commands

Every command a worker or the orchestrator runs. Keep the **Purpose** column stable (the skills refer to L2/L3 by name).

| Purpose | Command |
|---|---|
| Build | `pnpm build` |
| Test — **verify L2 floor** | `pnpm lint && pnpm build` |
| Boot the app (visual loop) | `pnpm dev` → http://localhost:3000 |
| App screenshot | *None scripted.* Use the `claude-in-chrome` browser tools interactively against the running dev server. |
| Install deps | `pnpm install` |

**There is no test suite, no test runner, and no typecheck script.** That is deliberate, not a gap to fill on the side of an unrelated issue. `pnpm build` *is* the typecheck (Next runs `tsc` as a build step) and `pnpm lint` is the ESLint CLI (`next lint` was removed in Next 16, so `next build` no longer lints — both commands are needed). Baseline as of this file: `pnpm lint` exits 0 with 2 pre-existing `react-hooks/exhaustive-deps` warnings (`GlitchTitle.tsx`, `SideModal.tsx`); a worker must not introduce new ones and must not "fix" those two as drive-by scope.

## App facts

- **Next.js 16 App Router · React 19 · TypeScript 5.9 · Tailwind 3.4 · pnpm · Node 24.x.** Turbopack is the default bundler for both `dev` and `build` — there is no `--turbopack` flag and no webpack config.
- `PageProps` / `LayoutProps` are **globals generated by `next typegen`** (run automatically by `dev`/`build`), so `app/page.tsx` writes `PageProps<"/">` with no import. On a fresh clone or a confused editor, run `pnpm next typegen` — never hand-write those types.
- **Content is TypeScript modules under `public/`**, next to the images they reference (`public/journey-data.ts`, `public/projects/neon-place/features-list.ts`). There is no CMS. A copy or list change is a code change to those files or to the JSX in `app/_sections/`.
- Path alias `@/*` maps to the repo root (`@/ui/atoms`, `@/public/journey-data`).
- **The scroll-driven body background is the fragile part of this codebase.** `document.body.style.backgroundColor` is mutated imperatively from two places that interact: `app/_sections/first-things-first.tsx` (an `IntersectionObserver` plus hand-tuned `scrollY < 500` / `scrollY > innerHeight * 3` thresholds and a `pink-background` body class) and `FancySectionTitle` (a 300vh scroll-scrubbed title that recomputes alpha per scroll event and sets/clears the same body background at 50%). Those thresholds are tuned to the *current* section order and heights. **Any change to section order, section height, or the addition of a full-height section will silently break the background transitions and the sticky/z-index stacking** — and cannot be caught by `lint` or `build`. Such an issue is L3 by definition (see the ladder) and carries an explicit visual-scroll QA note.
- `app/_sections/skills.tsx` exists but is not rendered by `page.tsx` — currently unused. Don't wire it in as a side effect.

## Verify ladder

- **L2 — floor, every issue, non-negotiable**: `pnpm lint && pnpm build` both pass, with no new lint warnings beyond the two-warning baseline above.
- **L3 — user-visible issues**: L2 + `pnpm dev` boots + the change is **visually confirmed in the browser at http://localhost:3000, including a full scroll pass top-to-bottom** when the issue touches layout, section order, section height, `StickySection`, `FancySectionTitle`, or `first-things-first.tsx`. Evidence = a screenshot captured with the `claude-in-chrome` tools. There is no scripted screenshot command, so this step is interactive by design.
- **L4** (agent-driven interaction): out of scope v1.
- **L5 — human**: once per PRD, via the QA doc, on the branch, before merge. Non-optional here — the scroll/background behaviour above has no automated coverage, so a human scroll-through is the only real gate on it.

## QA doc convention

- Path: `docs/qa/prd-<n>.md` (`<n>` = PRD issue number), committed on the PRD branch, linked from the PR body.
- Per issue: what shipped · how to test in the running app (from the issue's `## QA notes`, refined by the worker) · edge cases the worker flagged.
- The human runs it start-to-finish before merging the PR. For anything that touched layout, the run must include a full-page scroll on both desktop and a narrow viewport.

## Sources of truth (`grill-me` recon + hard gates)

- **Project explorer agent**: `Explore` — read-only, one section/area per spawn. There is no repo-specific explorer.
- **Contract-boundary explorer agent**: **None** — no API, no service boundary. This is a static-rendered marketing site.
- **Design / token map**: `tailwind.config.js` is the token source — brand colors `lk-green` `#50F900`, `lk-pink` `#FF034F`, `lk-turquoise` `#12E5E5`, `lk-blue` `#060CFF`, `lk-text-secondary` `#475460`. **Trap:** several of those hexes are also written *literally* in `app/globals.css` (carousel scrollbar, glitch pseudo-elements) — changing a brand color means changing both. Conventions live in `CLAUDE.md`, not in a ui-profile skill. **There is no Figma file and no Figma → code map** — `grill-me`'s Token Manifest resolves against `tailwind.config.js` alone, and a value with no Tailwind equivalent is a finding to surface, not a license to drop in a raw palette class.
- **Access-policy source**: **None** — static site, no data layer, no auth, no user-scoped rows.

## Repo discipline

- **All text styling goes through `Typography`** (`ui/atoms`, a `class-variance-authority` component with `p | p-sm | p-xs | h1 | h2 | h5 | h6 | link | small` variants that also picks the rendered tag). Because each variant sets a color, call sites override with `!` important utilities (`!text-white`, `!text-2xl`). **That is the established convention, not a smell** — do not "clean it up".
- **The aesthetic is deliberate.** Cryptic Unicode glyphs (`⏃⎑⎅▽`, `✺`, `⎔`, `⌿`) and vowels swapped for `ø`/`ā`/`ł` (`make_cøntact`, `Skiłłs`, `jøurney`) are intentional design choices. **Never normalize, "fix", or spell-correct them**, and never let a linter or a copy edit quietly do it.
- **Barrel files are partial on purpose.** `ui/atoms/index.ts` and `ui/components/index.ts` export most things, but `FancySectionTitle`, `CasesCarousel`, and `GlitchTitle` are **not** in the components barrel and are imported by full path. Match the convention of the file you're editing; adding a component means also adding it to the barrel only if you want the short import.
- **CONTEXT.md**: this repo has **no scoped `CONTEXT.md` files**. `CLAUDE.md` at the repo root is the single architecture doc — read it before touching `app/_sections/` or `ui/`, and update it when documented architecture changes (especially the scroll-background section).
- `README.md` is Liam's **GitHub profile README** — public-facing personal content, not project documentation. Never rewrite it as a setup guide.
- **Where skills live**: `.claude/skills/`. **Every skill directory there is a symlink** into `/Users/klyneker/liam-klyneker/skills/` (the canonical `LiamKlyneker/skills` repo). Writing a new file "into" `.claude/skills/<skill>/` therefore writes into the canonical repo — **never create or edit skill files through those symlinks**. This `_shared/` directory is the exception: it is a **real directory** holding **copies**, per the copy-not-symlink rule above.
- **Git workflow**: solo repo, `main` is the working branch — routine work commits and pushes straight to `main`, no PR ceremony. **`work-on-prd` is the exception**: it maintains one PRD branch and one PR targeting `main`, because `Closes #N` only fires against the default branch. Follow whichever mode the task is actually in; don't push a PRD branch's commits to `main` directly.

## One-time repo preconditions (human)

- GitHub Settings → General → "Auto-close issues with merged linked pull requests" must be **on** (not API-queryable — check in the web UI once). If off, `Closes #N` silently does nothing.
